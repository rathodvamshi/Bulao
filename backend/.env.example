# Authentication secrets belong only in Cloudflare Worker secrets.
# Do not copy this file into .dev.vars or bundle it into the mobile app.
# Required Worker secrets: MSG91_AUTH_KEY, AUTH_HASH_KEY (random, at least 32 characters).
# The public MSG91 template ID is configured in wrangler.toml.
